

  
<%= form_with(model: advertise) do |form| %>
  <% if flag==1 %>
 
  <% if advertise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(advertise.errors.count, "error") %> prohibited this advertise from being saved:</h2>

      <ul>
        <% advertise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= form.label :image_urls %>
  <%= form.text_field :image_urls,:maxlength=>"100" %>
</div>
<div class="field">
<%= form.label :Title %>
<%= form.text_field :Title,:maxlength=>"100" %>
</div>

<div class="field">
  <%= form.label :text %>
  <%= form.text_area :text,cols: 100, rows: 15 %>
</div>

<div class="field">
  <%= form.number_field :user_id,id: :advertise_user_id, value:current_user.id, type: :hidden %>
</div>

<div class="field">
<%= form.text_field :commentbox,id: :advertise_commentbox, value:"Comments", type: :hidden %>
</div>
<div class="actions">  
<!--- <input type="submit" name="commit" value="Add Comment" data-disable-with="Update Advertise" /> --->
   <%= form.submit %> 
  </div>

<% else %>

<div class="field">
  <%= form.text_field :image_urls,:maxlength=>"100", type: :hidden %>
</div>

<div class="field">
<%= form.text_field :Title,:maxlength=>"100", type: :hidden %>
</div>

<div class="field" id ='formtext'>
  <%= form.text_field :text,cols: 100, rows: 15, type: :hidden %>
</div>

<div class="field">
  <%= form.number_field :user_id,id: :advertise_user_id, type: :hidden %>
</div>

<div class="field">
<%= form.text_area :commentbox,id: :advertise_commentbox, cols: 40, rows: 4, value:" " %>

</div>

  <div class="actions">  
  <input type="submit" name="commit" value="Add Comment" data-disable-with="Update Advertise" />
  <!--- <%= form.submit %> --->
  </div>
  <% end %>
<% end %>
